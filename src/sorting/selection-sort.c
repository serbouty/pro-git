#include <stdio.h>

/**
 * Selection sort.
 *
 *
 * Pseudocode:
 *
 * For i from 0 to n-1
 *      Find smallest number between numbers[i] and numbers[n-1]
 *      Swap smallest number with numbers[i]
 *
 * Notation:
 *
 * (n-1) + (n-2) + (n-3) + ... + 1
 * O(n²)
 * Ω(n²)
 * Θ(n²)
 *
 */
int main(void)
{
    /* Implementation */
}